export class Dimensions
{
    height: number
    width: number
    length: number


    constructor(height: number, width: number, length: number) {
    this.height = height;
    this.width = width;
    this.length = length;
  }
}